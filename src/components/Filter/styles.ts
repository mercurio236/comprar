import { StyleSheet } from 'react-native'

export const s = StyleSheet.create({
  container: {
    flexDirection:'row',
    alignItems:'center',
    gap: 5
  },
  title: {
    fontSize: 12,
    fontWeight: '600',
  },
})
